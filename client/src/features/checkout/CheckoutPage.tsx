import { Typography } from "@mui/material";
import React from "react";

export const CheckoutPage = () => {
  return (
    <Typography variant="h3">Only logged users can see this page</Typography>
  );
};
